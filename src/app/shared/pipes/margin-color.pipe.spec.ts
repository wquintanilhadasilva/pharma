import { MarginColorPipe } from './margin-color.pipe';

describe('MarginColorPipe', () => {
  it('create an instance', () => {
    const pipe = new MarginColorPipe();
    expect(pipe).toBeTruthy();
  });
});
